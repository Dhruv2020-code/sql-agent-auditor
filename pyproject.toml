[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sql-agent-auditor"
version = "0.1.0"
description = "SQL Agent Auditor for OpenEnv Hackathon"
dependencies = [
    "fastapi",
    "uvicorn",
    "openai",
    "requests",
    "pandas",
    "python-multipart"
]
